server:
  port: 9004
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/puhongyu/config.git          #git仓库位置
          username: 15680609933                               #git仓库用户名
          password: phy286964071                              #git仓库用户密码
          #search-paths:                                       #配置仓库路径下的相对搜索位置
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

